import { Metadata } from "next";

import AboutContent from "@/components/content/AboutContent";

export const metadata: Metadata = {
  title: "About Us | Falls Monument | (920) 467-4939",
};

export default function About() {
  return <AboutContent />;
}
